% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occurrence.R
\name{occurrence_db}
\alias{occurrence_db}
\title{Find occurrence records from the OBIS open-data parquet dataset}
\usage{
occurrence_db(
  connection,
  scientificname = NULL,
  taxonid = NULL,
  datasetid = NULL,
  nodeid = NULL,
  instituteid = NULL,
  areaid = NULL,
  startdate = NULL,
  enddate = NULL,
  startdepth = NULL,
  enddepth = NULL,
  geometry = NULL,
  measurementtype = NULL,
  measurementtypeid = NULL,
  measurementvalue = NULL,
  measurementvalueid = NULL,
  measurementunit = NULL,
  measurementunitid = NULL,
  redlist = NULL,
  hab = NULL,
  wrims = NULL,
  extensions = NULL,
  hasextensions = NULL,
  mof = NULL,
  dna = NULL,
  absence = NULL,
  event = NULL,
  dropped = NULL,
  flags = NULL,
  exclude = NULL,
  fields = NULL,
  qcfields = NULL,
  verbose = FALSE
)
}
\arguments{
\item{connection}{a \code{robisdb_conn} object created with \code{\link[=connect]{connect()}} (or
\code{\link[=connect_opendata]{connect_opendata()}}/\code{\link[=connect_opendata_local]{connect_opendata_local()}}).}

\item{scientificname}{the scientific name. Matched against \code{interpreted.scientificName}
as well as every taxonomic rank column (kingdom, phylum, class, order, family,
genus, species), so higher taxon names (e.g. a family) work as well.}

\item{taxonid}{the taxon identifier (WoRMS AphiaID). Only matches the resolved,
lowest-rank AphiaID stored per record (\code{interpreted.aphiaid}).}

\item{datasetid}{the dataset identifier.}

\item{nodeid}{the OBIS node identifier.}

\item{instituteid}{not available in the open-data dataset; ignored with a warning.}

\item{areaid}{not available in the open-data dataset; ignored with a warning.}

\item{startdate}{the earliest date on which occurrence took place.}

\item{enddate}{the latest date on which the occurrence took place.}

\item{startdepth}{the minimum depth below the sea surface.}

\item{enddepth}{the maximum depth below the sea surface.}

\item{geometry}{a WKT geometry string.}

\item{measurementtype}{the measurement type to be included in the measurements data.}

\item{measurementtypeid}{the measurement type ID to be included in the measurements data.}

\item{measurementvalue}{the measurement value to be included in the measurements data.}

\item{measurementvalueid}{the measurement value ID to be included in the measurements data.}

\item{measurementunit}{the measurement unit to be included in the measurements data.}

\item{measurementunitid}{the measurement unit ID to be included in the measurements data.}

\item{redlist}{include only records with an IUCN Red List category assigned.}

\item{hab}{not available in the open-data dataset; ignored with a warning.}

\item{wrims}{not available in the open-data dataset; ignored with a warning.}

\item{extensions}{which extensions to include (\code{MeasurementOrFact}, \code{DNADerivedData}).}

\item{hasextensions}{which extensions need to be present.}

\item{mof}{include measurements data (shorthand for \code{extensions = "MeasurementOrFact"}).}

\item{dna}{include DNA data (shorthand for \code{extensions = "DNADerivedData"}).}

\item{absence}{only include absence records (\code{TRUE}), exclude them (\code{NULL}, default)
or include both (\code{"include"}).}

\item{event}{not available in the open-data dataset; ignored with a warning.}

\item{dropped}{only include dropped records (\code{TRUE}), exclude them (\code{NULL}, default)
or include both (\code{"include"}).}

\item{flags}{quality flags which need to be set.}

\item{exclude}{quality flags to be excluded from the results.}

\item{fields}{Darwin Core fields (from \code{interpreted}) to be included in the results.
Defaults to a curated set of commonly used fields (a fixed list, since unlike the
OBIS API this does not vary per query); pass \code{"all"} to include every field instead.}

\item{qcfields}{include the \code{missing} and \code{invalid} quality-control field lists.}

\item{verbose}{logical. Print the SQL query before running it (default = \code{FALSE}).}
}
\value{
The occurrence records in a \code{tibble}, mirroring \code{\link[robis:occurrence]{robis::occurrence()}}'s output
(unprefixed, lower/camelCase Darwin Core field names; \code{mof}/\code{dna}, when requested,
are list-columns of data frames that can be unnested).
}
\description{
Mirrors \code{\link[robis:occurrence]{robis::occurrence()}}, but instead of paging through the OBIS API it
builds a single SQL query against the \code{obis} view created by \code{\link[=connect]{connect()}} on top
of the OBIS open-data parquet dataset
(see \url{https://github.com/iobis/obis-open-data}). This is generally much faster
for bulk downloads, but a few API-only filters have no equivalent column in the
parquet schema and are ignored with a warning: \code{instituteid}, \code{areaid}, \code{hab},
\code{wrims} and \code{event}.
}
