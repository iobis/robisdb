% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{sync_public_s3_folder_cli}
\alias{sync_public_s3_folder_cli}
\title{Sync a public, unauthenticated S3 folder to a local directory, via the AWS CLI}
\usage{
sync_public_s3_folder_cli(bucket, s3_prefix, local_dir)
}
\arguments{
\item{bucket}{the S3 bucket name.}

\item{s3_prefix}{the folder (key prefix) within the bucket to sync.}

\item{local_dir}{local destination directory (created if it doesn't exist).}
}
\value{
\code{NULL}, invisibly (called for its side effect of downloading files).
}
\description{
Same end result as \code{\link[=sync_public_s3_folder]{sync_public_s3_folder()}} (downloads every object under
\code{s3_prefix} in \code{bucket} into \code{local_dir}), but shells out to \verb{aws s3 sync --no-sign-request} instead of using \code{paws}. Requires the
\href{https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html}{AWS CLI}
to be installed and on \code{PATH}; aborts with a clear message if it isn't. \verb{aws s3 sync}
is often faster and more robust for a dataset this size (parallel transfers, built-in
retry), and needs no AWS credentials (\code{--no-sign-request}), since OBIS's data buckets
are public.
}
