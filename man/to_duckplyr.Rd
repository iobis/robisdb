% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckplyr.R
\name{to_duckplyr}
\alias{to_duckplyr}
\title{Wrap a connection as a lazy duckplyr tibble (experimental)}
\usage{
to_duckplyr(connection, table = "obis", unnest_fields = TRUE)
}
\arguments{
\item{connection}{a \code{robisdb_conn} object.}

\item{table}{the view/table name to wrap (default \code{"obis"}).}

\item{unnest_fields}{flatten \code{interpreted}/\code{source}/\code{extensions} one level via
\code{dd::unnest()} (default \code{TRUE}; only applies to open-data connections).}
}
\value{
a lazy \code{duckplyr} tibble, or (if construction fails) whatever \code{try()} returns.
}
\description{
An escape hatch for exploring a connection with familiar \code{dplyr} syntax, evaluated as
an \emph{evidence-based decision}, not an oversight: \code{duckplyr} doesn't cope with this
dataset's deeply nested \code{interpreted}/\code{source}/\code{extensions} struct columns well enough
to replace the \code{select_db()}/\code{mutate_db()}/\code{filter_db()}/... verbs in \code{R/manual.R},
which remain the supported query path. See that file's \verb{@description} for the reasoning.

This function wraps \code{table} (default \code{"obis"}) as a \code{duckplyr::as_duckdb_tibble()}
lazy tibble and, for open-data connections with \code{unnest_fields = TRUE} (the default),
flattens \code{interpreted}/\code{source}/\code{extensions} one level via \code{dd::unnest()} so their
fields can be referenced as plain (unprefixed) column names in subsequent \code{dplyr} verbs
-- e.g. \code{to_duckplyr(con) |> filter(depth > 10) |> select(scientificName, depth)}.

\code{extensions} unnesting only promotes its raw URI-keyed fields (e.g.
\code{`http://rs.iobis.org/obis/terms/ExtendedMeasurementOrFact`}) one level; it does
not flatten each measurement/DNA record's \code{source} fields the way \code{occurrence_db()}'s
\code{mof}/\code{dna} columns do.

\strong{Use with care.} \code{duckplyr} silently materializes the \emph{entire} table into R memory
when it hits an operation (or, per the reasoning above, any of this package's own
geometry/H3/extension SQL) it can't translate -- there is no warning by default. Narrow
with \code{filter_db()}/\code{select_db()} (from \code{R/manual.R}) \emph{before} calling \code{to_duckplyr()};
never call this on a fresh, unfiltered connection over the full dataset.
}
