% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{connect}
\alias{connect}
\title{Connect to an OBIS parquet dataset}
\usage{
connect(con_type, path = NULL, select = NULL, where = NULL)
}
\arguments{
\item{con_type}{the dataset to connect to: one of \code{"open-data"}, \code{"speciesgrids"},
\code{"obistherm"}.}

\item{path}{path to a local copy of the dataset. If supplied, connects to the local
files instead of streaming from S3 (recommended -- see \code{\link[=connect]{connect()}}'s description).}

\item{select}{optional column list baked into the \code{obis} view's \code{select} clause
(defaults to \code{*}, i.e. every column).}

\item{where}{optional filter condition(s) baked into the \code{obis} view itself (applied
to every subsequent query against it).}
}
\value{
a \code{robisdb_conn} object.
}
\description{
Opens a DuckDB connection and creates a view named \code{obis} over one of OBIS's public
parquet datasets -- either read directly from its S3 bucket, or from a local copy (see
\code{\link[=sync_opendata]{sync_opendata()}}/\code{\link[=sync_speciesgrids]{sync_speciesgrids()}}/\code{\link[=sync_obistherm]{sync_obistherm()}}) when \code{path} is supplied.
All of the package's querying functions (\code{\link[=occurrence_db]{occurrence_db()}}, the \code{select_db()}/
\code{mutate_db()}/... verbs) work against this \code{obis} view.

\code{\link[=connect_opendata]{connect_opendata()}}, \code{\link[=connect_opendata_local]{connect_opendata_local()}}, \code{\link[=connect_speciesgrids]{connect_speciesgrids()}},
\code{\link[=connect_speciesgrids_local]{connect_speciesgrids_local()}}, \code{\link[=connect_obistherm]{connect_obistherm()}}, and \code{\link[=connect_obistherm_local]{connect_obistherm_local()}}
are convenience wrappers around \code{connect()} for each dataset.
}
