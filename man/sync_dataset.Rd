% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{sync_dataset}
\alias{sync_dataset}
\alias{sync_speciesgrids}
\alias{sync_obistherm}
\alias{sync_opendata}
\alias{sync_speciesgrids_cli}
\alias{sync_obistherm_cli}
\alias{sync_opendata_cli}
\title{Sync a local copy of an OBIS dataset}
\usage{
sync_speciesgrids(path)

sync_obistherm(path)

sync_opendata(path)

sync_speciesgrids_cli(path)

sync_obistherm_cli(path)

sync_opendata_cli(path)
}
\arguments{
\item{path}{local destination directory (created if it doesn't exist).}
}
\value{
\code{NULL}, invisibly (called for its side effect of downloading files).
}
\description{
Downloads (or updates) a local copy of one of OBIS's public datasets from S3.
\code{sync_speciesgrids()}, \code{sync_obistherm()}, and \code{sync_opendata()} use
\code{\link[=sync_public_s3_folder]{sync_public_s3_folder()}} (via \code{paws}, no extra software needed); the \verb{_cli}
variants use \code{\link[=sync_public_s3_folder_cli]{sync_public_s3_folder_cli()}} (via the AWS CLI's \verb{aws s3 sync}, if
installed -- often faster for a dataset this size). Once synced, connect to the local
copy with \code{\link[=connect_speciesgrids_local]{connect_speciesgrids_local()}}/\code{\link[=connect_obistherm_local]{connect_obistherm_local()}}/
\code{\link[=connect_opendata_local]{connect_opendata_local()}} (much faster than streaming from S3 directly).
}
