% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checklist.R
\name{checklist_db}
\alias{checklist_db}
\title{Find a taxon checklist from the OBIS open-data parquet dataset}
\usage{
checklist_db(
  connection,
  scientificname = NULL,
  taxonid = NULL,
  datasetid = NULL,
  nodeid = NULL,
  instituteid = NULL,
  areaid = NULL,
  startdate = NULL,
  enddate = NULL,
  startdepth = NULL,
  enddepth = NULL,
  geometry = NULL,
  redlist = NULL,
  hab = NULL,
  wrims = NULL,
  dropped = NULL,
  flags = NULL,
  exclude = NULL,
  verbose = FALSE
)
}
\arguments{
\item{connection}{a \code{robisdb_conn} object created with \code{\link[=connect]{connect()}} (or
\code{\link[=connect_opendata]{connect_opendata()}}/\code{\link[=connect_opendata_local]{connect_opendata_local()}}).}

\item{scientificname}{the scientific name. Matched against \code{interpreted.scientificName}
as well as every taxonomic rank column (kingdom, phylum, class, order, family,
genus, species), so higher taxon names (e.g. a family) work as well.}

\item{taxonid}{the taxon identifier (WoRMS AphiaID). Only matches the resolved,
lowest-rank AphiaID stored per record (\code{interpreted.aphiaid}).}

\item{datasetid}{the dataset identifier.}

\item{nodeid}{the OBIS node identifier.}

\item{instituteid}{not available in the open-data dataset; ignored with a warning.}

\item{areaid}{not available in the open-data dataset; ignored with a warning.}

\item{startdate}{the earliest date on which occurrence took place.}

\item{enddate}{the latest date on which the occurrence took place.}

\item{startdepth}{the minimum depth below the sea surface.}

\item{enddepth}{the maximum depth below the sea surface.}

\item{geometry}{a WKT geometry string.}

\item{redlist}{include only records with an IUCN Red List category assigned.}

\item{hab}{not available in the open-data dataset; ignored with a warning.}

\item{wrims}{not available in the open-data dataset; ignored with a warning.}

\item{dropped}{only include dropped records (\code{TRUE}), exclude them (\code{NULL}, default)
or include both (\code{"include"}).}

\item{flags}{quality flags which need to be set.}

\item{exclude}{quality flags to be excluded from the results.}

\item{verbose}{logical. Print the SQL query before running it (default = \code{FALSE}).}
}
\value{
A \code{tibble} with one row per distinct taxon and a \code{records} count, sorted by
\code{records} descending.
}
\description{
Mirrors \code{\link[robis:checklist]{robis::checklist()}}, but instead of paging through the OBIS API it builds a
single SQL query against the \code{obis} view created by \code{\link[=connect]{connect()}} on top of the OBIS
open-data parquet dataset. Unlike \code{\link[=occurrence_db]{occurrence_db()}}, this returns one row per distinct
taxon matched by the filters (not one row per occurrence), with a \code{records} column
counting how many occurrence records that taxon has, sorted by \code{records} descending.

A few API-only filters have no equivalent column in the parquet schema and are ignored
with a warning: \code{instituteid}, \code{areaid}, \code{hab}, \code{wrims}. There is no \code{absence} parameter
(matching \code{\link[robis:checklist]{robis::checklist()}}, which doesn't have one either) -- absence records would
corrupt the \code{records} counts, so they are always excluded. There is no \code{ncbi_id} column
in the output (\code{\link[robis:checklist]{robis::checklist()}} has one): it's an external WoRMS-to-NCBI
cross-reference not present anywhere in the open-data parquet dataset.
}
