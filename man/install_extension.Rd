% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{install_extension}
\alias{install_extension}
\alias{install_community_extension}
\alias{install_h3}
\alias{install_a5}
\alias{install_httpfs}
\title{Install and load DuckDB extensions}
\usage{
install_extension(connection, name)

install_community_extension(connection, name)

install_h3(connection)

install_a5(connection)

install_httpfs(connection)
}
\arguments{
\item{connection}{a \code{robisdb_conn} (its underlying connection is used automatically)
or a raw \code{duckdb_connection}.}

\item{name}{the extension name, for \code{install_extension()}/\code{install_community_extension()}.}
}
\value{
the \code{duckdb_result} from the underlying \code{\link[DBI:dbSendQuery]{DBI::dbSendQuery()}} call.
}
\description{
\code{install_extension()}/\code{install_community_extension()} install (from DuckDB's core or
community extension repository, respectively) and load an extension by name.
\code{install_h3()}, \code{install_a5()}, and \code{install_httpfs()} are convenience wrappers around
these for extensions not loaded by default by \code{\link[=connect]{connect()}}: \code{install_h3()} for H3
hexagonal indexing, \code{install_a5()} for the A5 extension (plus ICU, which it depends
on), and \code{install_httpfs()} for reading remote (S3/HTTP) files -- already loaded
automatically by \code{\link[=connect]{connect()}} for non-local connections, so only needed manually for a
local connection that also needs to read remote files.
}
