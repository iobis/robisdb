% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manual.R
\name{filter_spatial_db}
\alias{filter_spatial_db}
\alias{filter_spatial_db.robisdb_conn}
\title{Spatial filtering}
\usage{
filter_spatial_db(x, ...)

\method{filter_spatial_db}{robisdb_conn}(x, geometry, buffer = NULL, column = "geometry", ...)
}
\arguments{
\item{x}{a \code{robisdb_conn}.}

\item{...}{unused.}

\item{geometry}{a single WKT geometry string.}

\item{buffer}{optional buffer distance to apply to \code{geometry} before intersecting
(in the units of \code{geometry}'s coordinate system -- degrees, for the unprojected
WGS84 data used throughout this package).}

\item{column}{the geometry column to filter on (default \code{"geometry"}, the dataset's
geometry column).}
}
\value{
the modified \code{robisdb_conn}.
}
\description{
Adds a spatial \code{WHERE} condition to the query: keep only records whose \code{column}
intersects \code{geometry} (a WKT string), optionally buffered first. This covers the two
common cases -- \code{ST_Intersects(column, ST_GeomFromText(geometry))}, and the same
wrapped in \code{ST_Buffer()} when \code{buffer} is supplied -- built with the \code{spatial}
extension, which \code{\link[=connect]{connect()}} always loads. For anything more elaborate, build the
condition yourself and pass it to \code{\link[=filter_db]{filter_db()}}.
}
