% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{sync_public_s3_folder}
\alias{sync_public_s3_folder}
\title{Sync a public, unauthenticated S3 folder to a local directory}
\usage{
sync_public_s3_folder(bucket, s3_prefix, local_dir)
}
\arguments{
\item{bucket}{the S3 bucket name.}

\item{s3_prefix}{the folder (key prefix) within the bucket to sync.}

\item{local_dir}{local destination directory (created if it doesn't exist).}
}
\value{
\code{NULL}, invisibly (called for its side effect of downloading files).
}
\description{
Downloads every object under \code{s3_prefix} in \code{bucket} into \code{local_dir}, skipping files
whose local copy already matches the remote size (a cheap up-to-date check, not a
full checksum). Connects anonymously (no AWS credentials needed, since OBIS's data
buckets are public). \code{\link[=sync_speciesgrids]{sync_speciesgrids()}}, \code{\link[=sync_obistherm]{sync_obistherm()}}, and \code{\link[=sync_opendata]{sync_opendata()}}
are wrappers around this for each OBIS dataset.
}
