% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{connect_duckdb}
\alias{connect_duckdb}
\title{Connect to an arbitrary local parquet dataset}
\usage{
connect_duckdb(path, select = NULL, where = NULL)
}
\arguments{
\item{path}{path (or glob) to the parquet file(s), passed to \code{read_parquet()}.}

\item{select}{optional column list baked into the \code{obis} view's \code{select} clause
(defaults to \code{*}, i.e. every column).}

\item{where}{optional filter condition(s) baked into the \code{obis} view itself (applied
to every subsequent query against it).}
}
\value{
a \code{robisdb_conn} object.
}
\description{
Like \code{\link[=connect]{connect()}}, but for a parquet path that isn't one of OBIS's three named
datasets -- e.g. a custom extract or a dataset produced outside this package. Creates
the same \code{obis} view (with \code{spatial}/\code{httpfs} loaded) so \code{\link[=occurrence_db]{occurrence_db()}} and the
\code{select_db()}/\code{mutate_db()}/... verbs work against it unchanged.
}
