% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manual.R
\name{h3_index}
\alias{h3_index}
\alias{h3_index_db}
\alias{h3_index_db.robisdb_conn}
\alias{h3_parent_db}
\alias{h3_parent_db.robisdb_conn}
\alias{h3_children_db}
\alias{h3_children_db.robisdb_conn}
\title{H3 hexagonal indexing}
\usage{
h3_index_db(x, ...)

\method{h3_index_db}{robisdb_conn}(x, lat, lng, resolution, name = "h3_index", ...)

h3_parent_db(x, ...)

\method{h3_parent_db}{robisdb_conn}(x, cell, resolution, name = "h3_parent", ...)

h3_children_db(x, ...)

\method{h3_children_db}{robisdb_conn}(x, cell, resolution, name = "h3_children", ...)
}
\arguments{
\item{x}{a \code{robisdb_conn}.}

\item{...}{unused.}

\item{lat, lng}{bare (unquoted) latitude/longitude column expressions, e.g.
\code{interpreted.decimalLatitude}.}

\item{resolution}{for \code{h3_index_db()}, the resolution to compute the cell at; for
\code{h3_parent_db()}/\code{h3_children_db()}, the (coarser/finer, respectively) resolution to
move \code{cell} to.}

\item{name}{the name of the resulting column.}

\item{cell}{a bare (unquoted) H3 cell column expression.}
}
\value{
the modified \code{robisdb_conn}.
}
\description{
Convenience wrappers around DuckDB's \code{h3} community extension (see
\url{https://duckdb.org/community_extensions/extensions/h3}) for building an H3 index and
navigating its resolution hierarchy, each appending one computed column (like
\code{\link[=mutate_db]{mutate_db()}}). Requires the extension to be installed/loaded first, once per
connection, via \code{\link[=install_h3]{install_h3()}}.

\code{h3_index_db()} computes the H3 cell for a lon/lat pair at \code{resolution} (a coarser
\code{resolution} covers a larger area). \code{h3_parent_db()}/\code{h3_children_db()} move an
existing H3 cell column up/down the resolution hierarchy -- \code{h3_children_db()}'s
result is a list-column, since one cell has multiple children.
}
